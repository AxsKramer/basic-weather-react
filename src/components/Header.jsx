import React from 'react'

const Header = ({title}) => (
        <header className='w-100'>
            <h1 className='text-white text-center py-3'>{title}</h1>
        </header>
);
 
export default Header;